export enum Status {
    Active = "ACTIVE",
    InActive = "INACTIVE",
}

export enum UserType {
    Admin = "ADMIN",
    Client = "CLIENT",
}

export enum LocationType {
    CITY = "CITY",
    DISTRICT = "DISTRICT",
    WARD = "WARD",
}
export enum PostFilterType {
    LIKE = "LIKE",
    COMMENT = "COMMENT",
    RATE = "RATE",
}
